/**
 * Indicates a type of control.
 * Each control type has its own control flow, from control event trigger
 * to control state handling.
 */
enum ControlType {
    KEYPRESS,
    MOUSEOVER,
    MOUSECLICK
}


export {
    ControlType
}